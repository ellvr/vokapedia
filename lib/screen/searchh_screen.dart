import 'package:flutter/material.dart';

class SearchhScreen extends StatelessWidget {
  const SearchhScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('searchxxxx')));
  }
}
